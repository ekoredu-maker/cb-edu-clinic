# V10.3 업그레이드 보고서

- 매칭 서비스 호환성 조건을 명시적 역할 매트릭스로 수정
- 자동매칭과 수동매칭 모두 비호환 조합 차단
- 분기별 실적 양식의 기준단가 표시를 실제 세션 구성에 맞는 라벨로 변경
- V9.5 마이그레이션 시 수업협력코칭 40분 / 학습코칭 50분으로 세션 분리 이관
- patchState에서 return 누락 시 draft를 자동 저장하도록 방어코드 추가
- 초기화 시 앱 캐시도 함께 정리하도록 반영
- Service Worker의 SKIP_WAITING 메시지 처리 추가
